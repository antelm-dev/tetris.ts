# Web target env (loaded by Vite for the default mode).
# Any key here is exposed to the renderer as `import.meta.env.<KEY>`.
# Override VITE_BASE if the web build is deployed under a sub-path.
VITE_BASE=/
