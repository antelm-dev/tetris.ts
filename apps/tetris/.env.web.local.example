# Local-only overrides for `pnpm -C apps/tetris dev:web` / `build:web`.
# Copy to `.env.web.local` (gitignored) — do not commit secrets.
VITE_ONLINE_MULTIPLAYER_UI=1
VITE_API_ORIGIN=http://localhost:3000
